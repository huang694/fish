# 小组开发协作规范
> 项目：珠海白蕉鲈鱼智慧养殖系统
> 目的：统一开发流程、代码风格、Git使用规范，减少冲突，方便小组协同与毕设答辩。

## 一、人员分工
所有组员在开发前确认自己负责的模块，在文档/提交记录中体现；
模块之间的接口约定，需要提前在小组内沟通确认，避免前后端对接冲突。

## 二、Git 分支管理规范
### 分支说明
1. `main`：主分支，存放最终稳定、可部署上线的代码。**禁止直接push代码到main**。仅经过测试、小组确认完成的版本才合并进来。
2. `dev`：开发主分支，所有功能开发基于该分支拉取新分支；日常开发代码合并到dev。
3. `feature/模块名`：功能开发分支。例如：`feature/water-data` 水质数据模块、`feature/user-manage` 用户管理模块。
    - 从 `dev` 分支新建；
    - 功能开发完成、自测通过后，提交Pull Request（PR）合并回`dev`；
4. `hotfix/问题描述`：紧急bug修复分支。线上/演示版本出现问题时使用，修复完成同时合并到main和dev。

### 分支使用流程
1. 拉取最新 dev 代码：`git pull origin dev`
2. 创建功能分支：`git checkout -b feature/xxx`
3. 在功能分支开发、自测
4. 开发完成，提交PR，组员简单Review后合并到dev
5. 合并完成后，删除本地和远程feature分支

## 三、Commit 提交信息规范
> 提交格式：`类型: 简短描述`
类型可选：
- `feat`：新增功能
- `fix`：修复bug
- `docs`：文档修改（README、需求、接口文档等）
- `style`：代码格式调整，无逻辑变更（空格、换行，不改动业务）
- `refactor`：代码重构，既不是新增功能也不是修复bug
- `test`：新增/修改测试代码
- `chore`：构建、依赖、配置文件修改（pom.xml、gitignore等）

示例：
